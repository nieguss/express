extends layout

block content
  h1= title
  if show
    form(method='post')
      p Którą markę samochodów uważasz za lepszą?
      ul 
        each item in data 
          li  
            input(type='radio', name='quiz' value=item._id) 
            label=item.title 
      input(type='submit' value="Zagłosuj")
      p Aby zobaczyć wyniki oddaj głos.
  else 
    h3 Dokonałeś już głosu w ankiecie.
    p Wyniki
    ul
      each item in data
        li #{item.title}: #{item.vote} (#{Math.round(item.vote/sum*100)}%)
    
    p Oddano głosów: #{sum}